ENV['COCOAPODS_DISABLE_STATS'] = 'true'

source 'https://cdn.jsdelivr.net/cocoa/'

install! 'cocoapods',
  :generate_multiple_pod_projects => true,
  :incremental_installation => true

platform :ios, '12.0'

target 'GYG Challenge' do
  use_frameworks!
  
  pod 'SDWebImage'
  pod 'Cosmos'
  
end
